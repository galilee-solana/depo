'use client'

import { useRouter } from 'next/navigation'

export default function Add_recipient_address() {
    const router = useRouter();

    return(
        <div>
            <h1>Default set recipient address page</h1>
        </div>
    );
}