'use client'

import { useRouter } from 'next/navigation'

export default function Add_options() {
    const router = useRouter();

    return(
        <div>
            <h1>Default option page</h1>
        </div>
    );
}