'use client'

import { useRouter } from 'next/navigation'

export default function Add_depositor_address() {
    const router = useRouter();

    return(
        <div>
            <h1>Default set depositor address page</h1>
        </div>
    );
}